// la palabra clave interface se utiliza para diferenciar la interface
public interface Prey {
    // los métodos creados en la interface deben ser sobreescritos en la clase que adopte la interface
    void run();
}
